<article id='races' class='col-sm-3'>
  <ul>
    {{#each race in controller}}
      {{#link-to 'race' race tagName='li' className='race'}}
        {{race.name}}
      {{/link-to}}
    {{/each}}
  </ul>
</article>

{{outlet}}
